CREATE TABLE Tbl_Product_Categories
(
	fld_category_id varchar(10) NOT NULL, -- Automatically generated identification code for product categories
	fld_category_description varchar(50), -- name of product brand for detailed categorization and grouping purposes 

	PRIMARY KEY (fld_category_id)
)